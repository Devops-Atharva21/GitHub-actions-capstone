name: PR Pipeline

on:
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

jobs:
  build-test:
    uses: ./.github/workflows/reusable-build-test.yml
    with:
      node_version: "20"
      run_tests: true

  dependency-review:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-request: read

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Check Depnedenices for Vulnerabilites
        uses: actions/dependency-review-action@v4
        with:
          fail-on-severity: critical

  pr-comment:
    runs-on: ubuntu-latest
    needs: build-test

    steps:
      - name: Print PR Summary
        run: |
          echo "PR checks passed for branch: ${{ github.head_ref }}"
